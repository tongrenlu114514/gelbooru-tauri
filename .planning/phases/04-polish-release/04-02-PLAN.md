---
phase: "04"
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
  - src-tauri/tauri.conf.json
autonomous: true
requirements:
  - REQ-4.3
  - REQ-4.4
user_setup: []

must_haves:
  truths:
    - "README.md exists at project root with installation, usage, and contributing sections"
    - "tauri.conf.json version is 1.0.0 and NSIS installer is configured correctly"
  artifacts:
    - path: README.md
      provides: Project documentation
      min_lines: 80
      contains:
        - "## Features"
        - "## Installation"
        - "## Usage"
        - "## Configuration"
        - "## Contributing"
    - path: src-tauri/tauri.conf.json
      provides: Production release config
      contains:
        - '"version": "1.0.0"'
        - '"productName": "Gelbooru Downloader"'
        - '"targets": ["nsis"]'
        - '"installMode": "currentUser"'
  key_links:
    - from: README.md
      to: src-tauri/tauri.conf.json
      via: References correct product name and installer info
      pattern: "Gelbooru Downloader.*1.0.0"
---

<objective>
Create basic README.md (REQ-4.3) and verify tauri.conf.json production configuration (REQ-4.4). Both are independent docs/config tasks that run after Plan 01 completes.
</objective>

<context>
@src-tauri/tauri.conf.json
@src-tauri/Cargo.toml
</context>

<interfaces>
<!-- No new interfaces created by this plan. README is a new file, tauri.conf.json is read-only verification. -->

From src-tauri/tauri.conf.json (existing config to verify):
```json
{
  "productName": "Gelbooru Downloader",
  "version": "1.0.0",
  "identifier": "com.gelbooru.downloader",
  "bundle": {
    "targets": ["nsis"],
    "windows": { "nsis": { "installMode": "currentUser", "languages": ["SimpChinese", "English"] } }
  }
}
```

From src-tauri/Cargo.toml:
```toml
[package]
name = "gelbooru-tauri"
version = "1.0.0"
description = "Gelbooru Image Downloader"
edition = "2021"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create basic README.md at project root</name>
  <files>README.md</files>
  <action>
    Create `README.md` at the project root (E:\project\gelbooru\README.md) with the following content in English. Per D-05: 1-2 pages, basic README only. No screenshots, architecture diagrams, or FAQ.

    The README should use this exact structure and content:

    ```markdown
    # Gelbooru Downloader

    A desktop application for searching and downloading images from Gelbooru.

    ## Features

    - Search images by tags using Gelbooru's API
    - Download images with concurrent download control
    - Local gallery browser with folder tree navigation
    - Favorite tags management with hierarchy support
    - Configurable download path and proxy settings
    - Responsive dark theme UI (naive-ui)

    ## Prerequisites

    - [Node.js](https://nodejs.org/) 18 or higher
    - [Rust](https://www.rust-lang.org/) 1.70 or higher
    - [pnpm](https://pnpm.io/) 8 or higher

    ## Installation

    ### Build from source

    ```bash
    # Clone the repository
    git clone &lt;repository-url&gt;
    cd gelbooru

    # Install frontend dependencies
    pnpm install

    # Run in development mode
    pnpm tauri dev
    ```

    ### Build release

    ```bash
    pnpm tauri build
    ```

    The installer will be created at:
    `src-tauri/target/release/bundle/nsis/Gelbooru Downloader 1.0.0.exe`

    ## Usage

    ### First launch

    On first launch, set your preferred download path in **Settings**.

    ### Search and download

    1. Enter tags in the search bar (e.g., `rating:safe solo`)
    2. Browse results and click the download button on images you want
    3. Downloads appear in the **Downloads** tab

    ### Local gallery

    Switch to the **Gallery** tab to browse your downloaded images. Use the folder tree to navigate, or search within your local collection.

    ### Favorite tags

    Use the **Favorite Tags** tab to save commonly used tag combinations. Hierarchical tags are supported for organizing tag groups.

    ## Configuration

    Settings are accessible via the Settings tab:

    | Option | Default | Description |
    |--------|---------|-------------|
    | Download path | (empty) | Where images are saved |
    | Concurrent downloads | 3 | Max simultaneous downloads |
    | Proxy enabled | true | Use system proxy |
    | Proxy host | 127.0.0.1 | Proxy server address |
    | Proxy port | 7897 | Proxy server port |
    | Theme | dark | UI color theme |

    ## Contributing

    ### Development setup

    ```bash
    # Install dependencies
    pnpm install

    # Run linter
    pnpm lint

    # Run tests
    pnpm test        # frontend tests
    cargo test       # backend tests

    # Run full test suite
    pnpm vitest run &amp;&amp; cargo test

    # Build for release
    pnpm tauri build
    ```

    ### Code style

    - Frontend: ESLint + Prettier (run `pnpm lint:fix` to auto-fix)
    - Backend: rustfmt + clippy (run `cargo fmt &amp;&amp; cargo clippy -- -D warnings`)

    ## License

    MIT
    ```

    **What NOT to include** (per D-05 deferred ideas):
    - Screenshots or visual references
    - Architecture diagram
    - FAQ section
    - Changelog (not yet applicable for v1.0)
  </action>
  <verify>
    <automated>test -f "E:/project/gelbooru/README.md" && wc -l &lt; "E:/project/gelbooru/README.md" | awk '{exit !($1 >= 80)}'</automated>
  </verify>
  <acceptance_criteria>
    - test -f README.md returns 0 (file exists)
    - grep "## Features" README.md returns 1 match
    - grep "## Installation" README.md returns 1 match
    - grep "## Usage" README.md returns 1 match
    - grep "## Configuration" README.md returns 1 match
    - grep "## Contributing" README.md returns 1 match
    - grep "pnpm tauri build" README.md returns 1 match
    - grep "Gelbooru Downloader 1.0.0" README.md returns 1 match
    - grep "rating:safe solo" README.md returns 1 match
    - wc -l README.md returns >= 80 lines
  </acceptance_criteria>
  <done>
    README.md created at project root with Features, Installation, Usage, Configuration, and Contributing sections. Written in English, 1-2 pages, no screenshots or architecture diagrams.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tauri.conf.json production configuration</name>
  <files>src-tauri/tauri.conf.json</files>
  <action>
    Read `src-tauri/tauri.conf.json` and verify it is production-ready per D-06. No changes should be made — this is a read-only verification with findings documented.

    Verify the following settings:

    1. **Version**: `"version": "1.0.0"` — must be present and set to "1.0.0"
    2. **Product name**: `"productName": "Gelbooru Downloader"` — correct
    3. **Identifier**: `"identifier": "com.gelbooru.downloader"` — valid reverse-domain
    4. **Bundle targets**: `"targets": ["nsis"]` — Windows NSIS installer configured
    5. **NSIS install mode**: `"installMode": "currentUser"` — per-user install
    6. **NSIS languages**: `"languages": ["SimpChinese", "English"]` — appropriate for audience
    7. **CSP**: `"csp"` field present with `default-src 'self'` — security hardening in place
    8. **Build devtools**: Not explicitly set — defaults to false in release builds (correct)

    If all checks pass, write a verification summary as a comment at the top of `src-tauri/tauri.conf.json`:
    ```json
    // Phase 4 (REQ-4.4) verification: Production-ready configuration confirmed.
    // - version: 1.0.0
    // - bundle.targets: nsis (Windows installer)
    // - nsis.installMode: currentUser (per-user install)
    // - security.csp: configured
    // No changes required per D-06.
    ```

    If any setting does not match expected values, report the discrepancy instead of making changes — D-06 says "no changes needed" so any deviation should be surfaced to the user.
  </action>
  <verify>
    <automated>grep '"version": "1.0.0"' src-tauri/tauri.conf.json && grep '"productName": "Gelbooru Downloader"' src-tauri/tauri.conf.json && grep '"targets": \["nsis"\]' src-tauri/tauri.conf.json && grep '"installMode": "currentUser"' src-tauri/tauri.conf.json && echo "VERIFIED: All tauri.conf.json checks pass"</automated>
  </verify>
  <acceptance_criteria>
    - grep '"version": "1.0.0"' src-tauri/tauri.conf.json returns 1 match
    - grep '"productName": "Gelbooru Downloader"' src-tauri/tauri.conf.json returns 1 match
    - grep '"targets": \["nsis"\]' src-tauri/tauri.conf.json returns 1 match
    - grep '"installMode": "currentUser"' src-tauri/tauri.conf.json returns 1 match
    - grep '"version": "1.0.0"' src-tauri/tauri.conf.json returns exactly 1 match (version field should not appear twice)
  </acceptance_criteria>
  <done>
    tauri.conf.json verified as production-ready. Version 1.0.0 confirmed, NSIS installer configured, all security settings in place. No changes needed per D-06.
  </done>
</task>

</tasks>

<threat_model>
Phase 4 adds no new security surface. README is static documentation. tauri.conf.json verification is read-only. No threat model changes needed per research findings.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Phase 4 adds no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | N/A | N/A | N/A | Phase 4 is polish-only, no new attack surface |

</threat_model>

<verification>
After Task 1: README.md exists with all required sections (>= 80 lines, contains Features/Installation/Usage/Configuration/Contributing)
After Task 2: tauri.conf.json verified with version 1.0.0, NSIS configured, all checks pass
Full suite: `cargo test --package gelbooru-tauri --lib &amp;&amp; pnpm test` (if frontend tests available)
</verification>

<success_criteria>
- [x] README.md exists at project root
- [x] README.md contains ## Features, ## Installation, ## Usage, ## Configuration, ## Contributing
- [x] README.md mentions `pnpm tauri build` and correct installer output path
- [x] README.md is in English, 1-2 pages (>= 80 lines, no screenshots or architecture diagrams)
- [x] tauri.conf.json version field is "1.0.0" (exactly 1 occurrence)
- [x] tauri.conf.json NSIS target configured with currentUser install mode
- [x] tauri.conf.json CSP security policy present
- [x] All tauri.conf.json verification grep checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-release/04-02-SUMMARY.md`
</output>

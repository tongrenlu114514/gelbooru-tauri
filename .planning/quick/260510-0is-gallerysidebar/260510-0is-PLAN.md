---
quick_id: "260510-0is"
phase: "quick"
plan: "01"
wave: 1
depends_on: []
type: execute
files_modified:
  - "src/views/Gallery.vue"
  - "src/views/GallerySidebar.vue"
autonomous: false
requirements: []
must_haves:
  truths:
    - "Folder list replaces sidebar — no 240px collapsible panel"
    - "Current directory's subfolders shown as horizontal flat list above image grid"
    - "Clicking a folder navigates into it and updates the flat list"
    - "Up navigation available via '..' entry in folder list"
  artifacts:
    - path: "src/views/Gallery.vue"
      provides: "Flat folder list, breadcrumb-style navigation, keyboard nav, preview modal"
    - path: "src/views/GallerySidebar.vue"
      provides: "Deleted — replaced by inline folder list"
  key_links:
    - from: "src/views/Gallery.vue"
      to: "src/views/GalleryCards.vue"
      via: "subdirs prop"
      pattern: "subdirs prop drives folder card rendering in grid"
    - from: "Gallery.vue loadImagesForDirectory"
      to: "subdirs ref"
      via: "result.subdirs"
      pattern: "subdirs populated from get_directory_images"
---

<objective>
Remove the collapsible sidebar (GallerySidebar.vue) and replace it with a flat, horizontal folder list rendered inside the content area above the image grid. Subdirectories are displayed as clickable entries separated by dividers, enabling navigation without a tree structure.
</objective>

<context>
From src/views/Gallery.vue (current):
- `<GallerySidebar>` in template, imported at line 25, passed treeData/selectedKey/loadingTree
- `n-layout has-sider` wraps both sidebar and content
- `subdirs: SubDirInfo[]` ref holds current directory's subdirs (from get_directory_images)
- `selectedKey: string | null` holds current path

From src/views/GallerySidebar.vue (to be deleted, 117 lines):
- Collapsible 240px NLayoutSider with NTree
- Uses settingsStore.sidebarCollapsed

From src/views/GalleryCards.vue:
- SubDirInfo interface: `{ path, name, imageCount, thumbnail? }`
- Renders subdirs as folder cards in the image grid

Key behavioral requirements:
1. Remove `n-layout has-sider` wrapper — content fills full width
2. Add horizontal `<div class="folder-list">` between path bar and GalleryCards
3. Show ".." entry if selectedKey is not empty (go up one level)
4. Show each subdir as a pill/button with folder icon + name + count, separated by `|`
5. Click ".." -> strip last path segment, call loadImagesForDirectory(parentPath)
6. Click subdir -> call enterSubdir(subdir)
7. Show loading skeleton for folder list while loadingImages
8. Remove GallerySidebar import and component usage
9. Remove loadingTree state (sidebar loading skeleton no longer needed)
10. Keep treeData and loadTree for initial directory load — but do not render tree

Interface contracts used from GalleryCards.vue:
```typescript
interface SubDirInfo {
  path: string;
  name: string;
  imageCount: number;
  thumbnail?: string;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify Gallery.vue — remove sidebar, add flat folder list</name>
  <files>src/views/Gallery.vue</files>
  <action>
Remove GallerySidebar import (line 25). Replace `n-layout has-sider` with a plain `n-layout` (no sider). Remove `<GallerySidebar>` component usage. Add a `<div class="folder-list">` section inside `n-layout-content` between path bar and GalleryCards, rendering:

1. Loading skeleton when loadingImages=true (show 4 skeleton pills)
2. ".." entry as first item when selectedKey is not empty (click to go up one level via parentPath)
3. Flat list of subdirs: each item = folder icon + name + count badge, separated by "|"
4. Click handler on ".." -> loadImagesForDirectory(parentPath) — derive parentPath by stripping last path segment
5. Click handler on subdir -> enterSubdir(subdir)

The folder list styling:
- `.folder-list` container: flex row, flex-wrap wrap, gap 0, align-items center
- Each `.folder-item`: display flex, align-items center, gap 8px, padding "4px 8px", cursor pointer, border-radius 4px, hover background #f0f0f0, transition background 0.15s
- `.folder-item` .folder-icon: NIcon size 14, color #f0a020 (FolderOutline)
- `.folder-item` .folder-name: font-size 13px, color #333
- `.folder-item` .folder-count: font-size 11px, color #999, background #f5f5f5, padding 1px 5px, border-radius 8px
- `.folder-divider`: char "|" color #ddd, font-size 12px, padding 0 2px, user-select none

Also remove these from script:
- Import from naive-ui: NLayoutSider, NTree (no longer needed in template)
- treeData ref (no longer needed — keep loadTree to call get_directory_tree but do not pass treeData anywhere)
- loadingTree ref (remove — no sidebar loading state needed)
- TreeNode interface (remove — no tree rendering)
- findNodeByKey function (no longer used after removing tree select)
- handleTreeSelect function (no longer used — replaced by enterSubdir)
- Sidebar-related computed: collapsed, SIDEBAR_WIDTH (unused)
- Import useSettingsStore (no longer used)
- Import FolderOutline, ChevronForwardOutline icons (no longer used in sidebar)

Keep: subdirs, images, selectedKey, loadTree, loadImagesForDirectory, enterSubdir, handleTreeSelect (rename to handleFolderSelect? — but actually handleTreeSelect can be removed entirely since it did tree traversal that is no longer needed. Replace its usage in the tree select path with direct enterSubdir calls).

Actually, keep `handleTreeSelect` but rename to `handleSubdirSelect` for clarity. It can call `loadImagesForDirectory` directly without tree traversal since we already have the subdir path in the list.

Update the path bar section — add current folder name display (not just path). When selectedKey is null (no folder selected), show "选择左侧文件夹进入" message or hide the folder list entirely.

The folder list should ONLY render when selectedKey is not null (i.e., a folder is open).
</action>
  <verify>
npm test -- --filter=Gallery 2>&1 | tail -20
</verify>
  <done>
Gallery.vue renders a flat horizontal folder list above the image grid, no sidebar, loadTree still called on mount to fetch initial tree data, folder list shows current directory subdirs, ".." entry navigates up, click handlers update selectedKey and load images
</done>
</task>

<task type="auto">
  <name>Task 2: Delete GallerySidebar.vue</name>
  <files>src/views/GallerySidebar.vue</files>
  <action>
Delete the file src/views/GallerySidebar.vue. Verify no other file references it (grep for 'GallerySidebar' in src/).
</action>
  <verify>
grep -r "GallerySidebar" src/ 2>/dev/null | grep -v ".spec.ts" | grep -v "node_modules"
</verify>
  <done>
GallerySidebar.vue no longer exists, no remaining import references
</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify all tests pass and keyboard nav works</name>
  <what-built>Full gallery view with flat folder list replacing sidebar</what-built>
  <how-to-verify>
1. Run: npm test -- --filter=Gallery
2. All tests pass (IntersectionObserver, LRU cache, refresh behavior)
3. In browser: navigate to Gallery, open a folder, verify:
   - Folder list appears above grid with subdir entries
   - ".." entry navigates up correctly
   - Clicking subdir shows its images and subdirs
   - ArrowLeft/Right still works in preview modal
   - No console errors
4. Check: no 240px sidebar visible, content fills full width
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
npm test -- --filter=Gallery 2>&1 | tail -5
npm run build 2>&1 | tail -10
</verification>

<success_criteria>
- GallerySidebar.vue deleted
- Gallery.vue has no GallerySidebar import or component usage
- Gallery.vue uses plain n-layout (no has-sider)
- Flat folder list renders above GalleryCards when selectedKey is set
- ".." entry navigates to parent directory
- Subdirs shown with folder icon + name + count, separated by "|"
- All 8 existing tests pass without modification
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/quick/260510-0is-gallerysidebar/260510-0is-SUMMARY.md`
</output>
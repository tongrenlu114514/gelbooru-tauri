---
phase: quick-260510-l7j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/gallery.rs
  - src/views/GalleryCards.vue
autonomous: true
requirements: []
must_haves:
  truths:
    - Images in current directory display in descending modification time order
    - No folder grouping — all images render directly in the masonry grid
    - Breadcrumb navigation still functions when clicking ancestor segments
  artifacts:
    - path: "src-tauri/src/commands/gallery.rs"
      provides: "get_directory_images_async sorts images by mtime desc"
      contains: "sort_by.*mtime"
    - path: "src/views/GalleryCards.vue"
      provides: "Flat image rendering in masonry, subdirs not rendered as cards"
      contains: "subdirs"
  key_links:
    - from: "src-tauri/src/commands/gallery.rs"
      to: "src/views/Gallery.vue"
      via: "invoke get_directory_images"
      pattern: "get_directory_images.*images.*subdirs"
---

<objective>
Remove folder grouping in the gallery view. Display all images in the current directory directly in the masonry grid, sorted by modification time (newest first). Breadcrumb navigation stays intact.
</objective>

<context>
@src-tauri/src/commands/gallery.rs  (lines 614-674: `get_directory_images_async`)
@src/views/GalleryCards.vue         (lines 39-42: `displayItems` composition)
@src/views/Gallery.vue              (lines 276-300: breadcrumb, lines 303-311: GalleryCards props)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sort images by modification time in backend</name>
  <files>src-tauri/src/commands/gallery.rs</files>
  <action>
    In `get_directory_images_async` (lines 614-674), the `images_with_time` vector is already collected with mtime.
    The current sort on line 659 (`images_with_time.sort_by(|a, b| b.1.cmp(&a.1));`) already sorts newest first.
    However, this only sorts images in the current directory (not recursively into subdirs).
    The task is to make the sort work the same way — but verify the current behavior is correct.
    Since the current code already sorts by mtime desc (b.1.cmp(a.1)), no Rust change is needed for sorting.
    However, the current behavior only shows direct images in the directory, not recursively from subfolders.
    Update `get_directory_images_async` to recursively collect ALL images under `dir_path` (not just direct children),
    sort the full list by modification time descending, and return only `images` with `subdirs` set to empty `Vec`.
    Remove the SubDirInfo collection logic from this function — no longer needed for display purposes.
    Keep the subdirs field in the response type but set it to empty (this preserves API compatibility if other parts depend on the response shape).
  </action>
  <verify>
    <automated>cargo test --package gelbooru gallery -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>get_directory_images command returns all images under dir_path sorted by mtime desc, subdirs=[], no folder grouping</done>
</task>

<task type="auto">
  <name>Task 2: Remove folder cards from GalleryCards masonry grid</name>
  <files>src/views/GalleryCards.vue</files>
  <action>
    In GalleryCards.vue, the `displayItems` computed (lines 39-42) mixes `subdirs` and `images` together.
    Since subdirs will now always be empty, remove them from displayItems — only show images.
    Change `displayItems` to return only `props.images.map(i => ({ ...i, _type: 'image' as const }))`.
    Remove the `v-if="item._type === 'folder'"` branch from the template (lines 83-100).
    Remove the `folder-card` class reference and `.folder-preview` styles (lines 191-222).
    Keep the image card rendering and all supporting logic (image error handling, lazy loading, filename hover).
    The `subdirs` prop and `SubDirInfo` type can remain declared (breadcrumb in Gallery.vue still passes them) but are not rendered.
  </action>
  <verify>
    <automated>pnpm vue-tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>GalleryCards renders only images, no folder cards; masonry grid shows flat image list sorted by time</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> Tauri command | `dir_path` string crosses from Vue to Rust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-l7j-01 | T (Tamper) | dir_path param | mitigate | Existing `validate_path` on line 292 blocks path traversal ("..") before fs read |

Threat model is unchanged from existing implementation — no new attack surface introduced by this change.
</threat_model>

<verification>
Backend: `cargo test get_directory_images` — images sorted by mtime desc, subdirs empty
Frontend: `pnpm vue-tsc --noEmit` — no type errors, no folder card references remaining
</verification>

<success_criteria>
- Gallery view shows images from current directory only (not subdirs), sorted by modification time newest-first
- No folder cards appear in the masonry grid
- Breadcrumb navigation continues to work (clicking ancestor segment navigates to that folder, which then shows its images)
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260510-l7j/260510-l7j-SUMMARY.md`
</output>